# 項目表

## 目錄用途

- `worker-send/`：Cloudflare Email Worker 專案
- `docker-accept/`：接收 webhook 的 Node.js / TypeScript 服務
- `.github/workflows/`：CI/CD，自動建置 Docker 映像
- `agent/`：本次接手紀錄與任務資訊

## 依賴概覽

### worker-send

- `wrangler`：Cloudflare Worker 開發與部署
- `typescript`：型別與編譯

### docker-accept

- `express`：HTTP API 與靜態頁面
- `typescript`：型別與編譯
- `tsx`：本地開發啟動
